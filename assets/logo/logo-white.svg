<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 500 500" width="500" height="500">
  <!-- Dark background -->
  <rect width="500" height="500" fill="#26332B"/>

  <!-- White outer ring -->
  <circle cx="250" cy="250" r="220" fill="none" stroke="#ffffff" stroke-width="1.2"/>

  <!-- Inner ring (refined feel) -->
  <circle cx="250" cy="250" r="212" fill="none" stroke="#ffffff" stroke-width="0.4" opacity="0.4"/>

  <!-- Placeholder text -->
  <text
    x="250"
    y="242"
    text-anchor="middle"
    font-family="'Inter', system-ui, sans-serif"
    font-size="11"
    fill="#ffffff"
    letter-spacing="2"
    opacity="0.6">Symbol</text>
  <text
    x="250"
    y="260"
    text-anchor="middle"
    font-family="'Inter', system-ui, sans-serif"
    font-size="11"
    fill="#ffffff"
    letter-spacing="2"
    opacity="0.6">placeholder</text>
</svg>
